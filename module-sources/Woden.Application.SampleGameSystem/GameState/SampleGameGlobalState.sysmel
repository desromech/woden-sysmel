namespace Woden definition: {
namespace GameSystem definition: {

class SampleGlobalGameState superclass: GlobalGameState; definition: {
    override method newPlayerEntity => PlayerRef
        := SamplePlayer new upCastFor: Player.
}.

compileTime constant SampleGlobalGameStateRef := SampleGlobalGameState rcRef.

}. ## namespace GameSystem
}. ## namespace Woden
