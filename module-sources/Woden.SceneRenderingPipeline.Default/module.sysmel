compileTime if: Compiler compilationTarget isDesktopPC || Compiler compilationTarget isWindowsFamily then: {
    Compiler importIndirectModulesNamed: #(
        #"Woden.SceneRenderingPipeline.Forward"
    ).
}.
